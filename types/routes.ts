export enum PRIVATE_ROUTES {
  ACCOUNT = "/account",
}

export enum PUBLIC_ROUTES {
  HOME = "/",
  LOGIN = "/login",
  REGISTER = "/register",
  PROJECTS = "/projects",
  NEW_PROJECT = "/projects/new",
  NEW_NOTE = "/notes/new",
}
